version: '2'

services:
  node:
    image: node:10.15.0
    container_name: nextjs-app
    working_dir: /project
    restart: always
    ports:
      - "8086:3000"
    tty: true
    volumes:
      - /Users/msuarez/Workspace/nextjs/:/project